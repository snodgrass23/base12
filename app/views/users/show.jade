extends ../layouts/layout

block subject
  header#subject.wrap
    - if (is_self)
      h1 Profile
    - else
      h1 Profile of #{user.name}

block content
  section#main.wrap
    dl
      dt Name:
      dd #{user.name}
      dt About:
      dd #{user.about}