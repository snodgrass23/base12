!!! 5
html
  head
    meta(charset="utf-8")
    title
      block title
        | New node project
    block styles
      link(rel='stylesheet', href='/styles/layout.css' )
    
  body(class = (is_logged_in) ? "logged-in" : "not-logged-in")
    
    != messages()

    header
      h1 
        a(href="/") Node Boilerplate
      #account
        - if (is_logged_in)
          aside
            p Welcome #{current_user.name}
              | &nbsp; - &nbsp; 
              a(href=account_route) Profile
              | &nbsp; - &nbsp; 
              a(href=account_route+'/edit') Account Settings
              | &nbsp; - &nbsp; 
              a(href=route('logout')) Logout
        - else
          aside
            p
              a(href=route('login')) Log in
              | &nbsp; or &nbsp; 
              a(href=route('register')) Create Account
    
    block content

    block foot
      footer
        p &copy;2011 Yeah, great footer!