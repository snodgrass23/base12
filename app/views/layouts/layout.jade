!!! 5
html
  != partial("components/layout/head")
    
  body(class = (is_logged_in) ? "logged-in" : "not-logged-in")
    
    != messages()

    header
      h1 
        a(href="/") Node Boilerplate
      #account
        - if (is_logged_in)
          aside
            p 
              a(href=account_route) Welcome #{current_user.name}
              | &nbsp; - &nbsp; 
              a(href=route('logout')) Logout
        - else
          aside
            p
              a(href=route('login')) Log in
              | &nbsp; or &nbsp; 
              a(href=route('register')) Create Account
    section
      != body  
    != partial("components/layout/foot")
    